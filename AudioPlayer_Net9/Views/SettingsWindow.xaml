<Window x:Class="AudioPlayer_Net9.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AudioPlayer_Net9.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="SettingsWindow" Height="250" Width="250">
    <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="50"></RowDefinition>
          <RowDefinition Height="*"></RowDefinition>
          <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center">Settings</TextBlock>
      <StackPanel Grid.Row="1">
	    <TextBlock>Select Music Folder</TextBlock>
			<TextBox Name="folderPath" Margin="10,5" Text="{Binding SettingsPath, UpdateSourceTrigger=PropertyChanged}"></TextBox>
			<Button HorizontalAlignment="Right" Margin="10, 5" Command="{Binding OpenFolderPicker}">Select Folder</Button>
		</StackPanel>
      <StackPanel Grid.Row="2" HorizontalAlignment="Center" Orientation="Horizontal">
        <Button Margin="10" Width="100" Command="{Binding SaveCommand}">Save</Button>
        <Button Margin="10" Width="100" Command="{Binding CancelCommand}">Cancel</Button>
      </StackPanel>
	</Grid>
</Window>
