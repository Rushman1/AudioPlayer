<UserControl x:Class="AudioPlayer_Net9.Views.ArtistListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AudioPlayer_Net9.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="100"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Margin="20,0,0,0">
			<TextBlock FontSize="32" FontWeight="Bold" Foreground="white" Text="Artists"></TextBlock>
		</StackPanel>
		<ListBox Grid.Row="1" 
		         ItemsSource="{Binding ArtistsView}" 
		         Background="Transparent" 
		         BorderThickness="0" Margin="15,0,0,0"
		         ScrollViewer.VerticalScrollBarVisibility="Hidden"
		         ScrollViewer.HorizontalScrollBarVisibility="Hidden">
			<ListBox.GroupStyle>

				<GroupStyle>
					<GroupStyle.HeaderTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Name}"
							           FontSize="18"
							           FontWeight="Bold"
							           Foreground="White"/>
						</DataTemplate>
					</GroupStyle.HeaderTemplate>
					<GroupStyle.Panel>
						<ItemsPanelTemplate>
							<WrapPanel Orientation="Vertical"></WrapPanel>
						</ItemsPanelTemplate>
					</GroupStyle.Panel>
				</GroupStyle>

			</ListBox.GroupStyle>

			<ListBox.ItemTemplate>

				<DataTemplate>
					<Button
						Command="{Binding DataContext.SelectArtistCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
						CommandParameter="{Binding}"
						Style="{StaticResource ArtistAlbumTrackButtonStyle}">
						<StackPanel>
							<Border Width="120"
							        Height="160"
							        Padding="5">
								<StackPanel>
									<!-- Album Art -->
										<Ellipse Width="100"
										         Height="100"
										         HorizontalAlignment="Center"
										         VerticalAlignment="Center">
											<Ellipse.Fill>
												<ImageBrush ImageSource="{Binding ImagePath}" Stretch="UniformToFill"/>
											</Ellipse.Fill>
										</Ellipse>
									<!-- Artist Name -->
										<TextBlock Text="{Binding Name}"
										           FontWeight="Bold"
										           Foreground="White"
										           Margin="5,8,0,0"
										           TextWrapping="Wrap"
										           TextAlignment="Center" />
									</StackPanel>
								</Border>
							</StackPanel>
						</Button>
				</DataTemplate>

			</ListBox.ItemTemplate>

		</ListBox>
	</Grid>
</UserControl>